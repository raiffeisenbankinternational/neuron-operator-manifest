# auto-generated by crdgen.py
# any changes will be over-written!
# source of truth is in neuron-operator-application/apis
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: neurontenants.neuron.isf
spec:
  group: neuron.isf
  names:
    kind: NeuronTenant
    plural: neurontenants
    singular: neurontenant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              lifecyclePolicy:
                default: KeepAfterDeletion
                description: The lifecycle policy to use for the resource
                enum:
                - KeepAfterDeletion
                - CleanUpAfterDeletion
                type: string
              neuronClusterName:
                description: The name of the Neuron cluster the resource should be
                  deployed to.
                type: string
              settings:
                description: Information of adminRoles and allowedClusters for tenant
                properties:
                  adminRoles:
                    description: Comma separated list of auth principal allowed to
                      administrate the tenant.
                    items:
                      type: string
                    type: array
                  allowedClusters:
                    description: Comma separated allowed clusters.
                    items:
                      type: string
                    type: array
                type: object
              tenant:
                description: Name of the tenant
                type: string
            required:
            - tenant
            type: object
          status:
            properties:
              conditions:
                default: []
                description: List of condition checks
                items:
                  properties:
                    message:
                      description: Message for a failed status check
                      type: string
                    reason:
                      description: Reason for a failed status check
                      type: string
                    status:
                      description: Status of condition check
                      type: string
                    type:
                      description: Type of condition check
                      type: string
                  required:
                  - type
                  - status
                  type: object
                type: array
              connectionURL:
                description: URL from the NeuronConnection referenced by ConnectionRef
                type: string
              observedGeneration:
                description: Last observed generation of resource
                type: integer
              phase:
                description: The phase of the resource
                type: string
            required:
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
